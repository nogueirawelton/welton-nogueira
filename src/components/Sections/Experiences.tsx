import { Box, Flex } from "@chakra-ui/react"

interface ExperiencesProps {
  
}

export const Experiences = ({  }: ExperiencesProps) => {
  return (
    <Flex id="experiences" as="section" h="100vh">
      <Box>

      </Box>
    </Flex>
  )
}